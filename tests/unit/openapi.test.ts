import {OpenAPI} from '../../dist/schema';

describe('OpenAPI OpenAPI Segment', () => {
  const fs = require('fs');
  const yaml = require('yaml');

  it('should ignore tests for now', () => {

  });

});